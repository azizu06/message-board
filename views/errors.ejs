<% if (errors?.length) { %>
  <div class="px-5 pt-6 sm:px-8">
    <ul class="rounded-2xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
      <% errors.forEach((err) => { %>
        <li><%= err.msg %></li>
      <% }); %>
    </ul>
  </div>
<% } %>
